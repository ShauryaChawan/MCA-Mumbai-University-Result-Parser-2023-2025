the following pdf has 6 pages. how do i iterate over single page?

```
const fs = require("fs");
const pdf = require("pdf-parse");

let dataBuffer = fs.readFileSync(
  "D:\\Projects\\PDF to Excel\\pdfs\\sem-1\\1T00161 - Sem 1-515-520.pdf"
);

pdf(dataBuffer)
  .then(function (data) {
    // number of pages
    console.log(data.numpages);
    // number of rendered pages
    console.log(data.numrender);
    // PDF info
    console.log(data.info);
    // PDF metadata
    console.log(data.metadata);
    // PDF.js version
    // check https://mozilla.github.io/pdf.js/getting_started/
    console.log(data.version);
    // PDF text
    console.log(data.text);

    // let extractedData = data.text.split(
    //   "----------------------------------------------------------------------------------------------------------------------------------"
    // );

    // // console.log(extractedData);
    // processAllStudentsData(extractedData);
  })
  .catch(function (error) {
    console.log(error + "Couldn't read the pdf !!");
  });

```

the above code give data of all the pages.